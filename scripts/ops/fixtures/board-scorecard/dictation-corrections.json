{
  "_comment": "Synthetic dictation correction cases. raw = spoken input as the recognizer hears it; expected = the processed output after dictionary corrections, filler cleanup, and punctuation. No private data. The candidate file (app output) is generated by driving dictation on the Mac and keyed by id.",
  "items": [
    {
      "id": "dict-001",
      "raw": "um so i pushed the uh the change to the api endpoint",
      "expected": "So I pushed the change to the API endpoint."
    },
    {
      "id": "dict-002",
      "raw": "lets sync the json payload with the s3 bucket you know",
      "expected": "Let's sync the JSON payload with the S3 bucket."
    },
    {
      "id": "dict-003",
      "raw": "the ci pipeline failed on the macos runner again",
      "expected": "The CI pipeline failed on the macOS runner again."
    },
    {
      "id": "dict-004",
      "raw": "i think we should like ship it on friday i mean monday",
      "expected": "I think we should ship it on Monday."
    }
  ]
}
