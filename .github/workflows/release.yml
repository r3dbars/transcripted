name: Release (disabled pending Draft public release pipeline)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled state
        run: |
          echo "The legacy Transcripted release workflow is intentionally disabled on main."
          echo "This repo now ships Draft on main, and a new public release pipeline must be"
          echo "reviewed before any post-cutover tag is used."
