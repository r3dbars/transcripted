# Usage & health visual evidence

These images render the production `UsageHealthPanel` with SwiftUI ImageRenderer at a 490-point width and 2x scale. Both use synthetic metadata; no customer activity, capture content, UUIDs, or device names are present.

- `usage-health-populated.png`: weekly metrics, all three quality outcomes, three synthetic failures, and privacy copy.
- `usage-health-empty.png`: first-use/cleared summary.

Reviewed for legibility, complete failure rows, wrapping, native system type, and clipped content. This is component-render evidence, not proof of a full macOS Settings click-through or live vendor ingestion.
