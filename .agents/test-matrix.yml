# Path-to-verification map for coding agents.
#
# This file is intentionally simple YAML so humans and agents can read it
# without a repo-specific parser. `scripts/dev/agent-preflight.sh` mirrors these
# rules when it prints suggested checks for the current branch.

rules:
  - paths:
      - "Sources/**/*.swift"
      - "Tests/*.swift"
      - "Tests/FastTests.manifest"
    checks:
      - "bash build.sh"
      - "bash run-tests.sh"

  - paths:
      - "Sources/Meeting/**"
      - "Sources/TranscriptedCore/**"
      - "Tests/Integration/**"
    checks:
      - "bash build.sh"
      - "bash run-tests.sh"
      - "bash run-integration-smoke.sh"

  - paths:
      - "Tests/E2E/**"
      - "run-e2e-smoke.sh"
      - "scripts/entrypoints/run-e2e-smoke.sh"
    checks:
      - "bash run-e2e-smoke.sh"

  - paths:
      - "scripts/ops/transcripted-qa-bench.sh"
      - "scripts/ops/validate-meeting-corpus.py"
      - "docs/qa-test-bench.md"
    checks:
      - "bash scripts/ops/transcripted-qa-bench.sh --mode quick --skip-build"
      - "bash scripts/ops/transcripted-qa-bench.sh --mode corpus"

  - paths:
      - "Tests/TranscriptedCoreTests/LiveCaptureSmokeTests.swift"
      - "run-live-capture-smoke.sh"
      - "scripts/entrypoints/run-live-capture-smoke.sh"
    checks:
      - "bash run-live-capture-smoke.sh --skip-build"

  - paths:
      - "Package.swift"
      - "Sources/TranscriptedCore/**"
      - "Tests/TranscriptedCoreTests/**"
    checks:
      - "bash build.sh"
      - "bash run-tests.sh"
      - "bash run-integration-smoke.sh"
      - "swift test"

  - paths:
      - "Sources/Observability/**"
      - "Info.plist"
      - "docs/sparkle-updates.md"
      - "docs/appcast.xml"
    checks:
      - "bash build.sh"
      - "bash run-tests.sh"

  - paths:
      - "build-beta.sh"
      - "scripts/entrypoints/build-beta.sh"
      - "scripts/release/**"
      - "docs/release-packaging.md"
      - "docs/sparkle-updates.md"
      - "Casks/**"
      - "docs/appcast.xml"
    checks:
      - "bash build.sh"
      - "bash run-tests.sh"
      - "SKIP_NOTARIZATION=1 bash build-beta.sh <token> <user-name>"

  - paths:
      - "Tools/TranscriptedCLI/**"
    checks:
      - "swift test --package-path Tools/TranscriptedCLI"

  - paths:
      - "Tools/TranscriptedMCP/**"
    checks:
      - "swift test --package-path Tools/TranscriptedMCP"

  - paths:
      - "Tools/TranscriptedQA/**"
    checks:
      - "swift test --package-path Tools/TranscriptedQA"

  - paths:
      - "README.md"
      - "AGENT_START.md"
      - "AGENTS.md"
      - "CLAUDE.md"
      - "CONTRIBUTING.md"
      - "docs/**"
      - ".agents/**"
    checks:
      - "scripts/dev/agent-preflight.sh"

notes:
  - "If a change matches multiple rules, run the union of checks."
  - "When in doubt, prefer the stricter AGENTS.md rule."
  - "Live capture smoke requires local microphone and System Audio Recording permissions; document permission blockers explicitly."
  - "UI changes should include sanitized review visuals under .agent-review/visuals/."
