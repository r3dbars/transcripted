# Screenshots

This folder should contain current public screenshots only.

`launch/` contains sanitized launch and social screenshots captured from the
current Transcripted app with demo-only meeting and dictation data.

Historical Draft-era screenshots live under `docs/archive/screenshots/`. If a
screen no longer represents the current Transcripted app on `main`, move it to
the archive instead of leaving it here.
